# 技術スタック

## フロントエンド

| 技術       | 用途                                                                   |
| ---------- | ---------------------------------------------------------------------- |
| TypeScript | 型安全なアプリケーション開発                                           |
| Vite       | ビルド・開発サーバー                                                   |
| React      | UI フレームワーク                                                      |
| Three.js   | 通常プレイの3Dジオラマ、河川・街・施設の描画、地表の位置取得           |
| CesiumJS   | 旧地理空間ビューの実装・検証用依存（通常プレイでは読み込まない）       |
| WebGL      | 3D レンダリング                                                        |
| WebSocket  | リアルタイム通信                                                       |
| PWA        | ホーム画面追加・オフラインキャッシュ（MVP 対象外・Phase 5 以降で検討） |
| HTML / CSS | マークアップ・スタイル                                                 |

通常プレイは Three.js の明るい3Dジオラマで表示します。対象は福島県郡山市の日本大学工学部周辺を流れる阿武隈川です。川筋と配置・治水計算の地理座標は保持し、街・植栽・護岸はゲーム用に簡略化しています。航空写真や実測の建物形状を忠実に再現するビューではありません。

描画エンジンは表示と入力を担当し、配置可否、水位、浸水、被災度の計算は既存のゲームサービスに分離します。旧 CesiumJS 実装は保持していますが、通常プレイとモード選択時のプリロードは `DioramaGameMap` を使用します。詳細は [地理空間アーキテクチャ](./geospatial.md) を参照してください。

## バックエンド

| 技術       | 用途                         |
| ---------- | ---------------------------- |
| Go         | バックエンドアプリケーション |
| REST API   | ゲーム前後の処理             |
| WebSocket  | ゲーム中のリアルタイム処理   |
| PostgreSQL | 永続データ                   |
| Redis      | セッション・キャッシュ       |

バックエンドでは、以下の処理を担当します。

- プレイヤー管理
- ルーム管理
- ゲーム状態管理
- リアルタイム同期
- 土木施設の設置判定
- 災害イベントの進行
- 水位や浸水範囲の計算
- 被災度の計算

初期段階では、モジュラーモノリスとして単一のバックエンドアプリケーションを構築します。

将来的には、アクセス数や負荷特性に応じて、API サーバーとゲームサーバーを分離できる設計を目指します。

## インフラ

初期構成では、以下のサービスや技術を候補とします。

| 技術                    | 用途                                       |
| ----------------------- | ------------------------------------------ |
| Docker / Docker Compose | ローカル・コンテナ実行                     |
| Cloudflare Pages        | ソロ静的 Web 公開（案α・低コスト）         |
| AWS                     | クラウド基盤（マルチ・永続化が必要な段階） |
| Amazon ECS              | コンテナオーケストレーション               |
| Amazon RDS              | マネージド PostgreSQL                      |
| Amazon ElastiCache      | マネージド Redis                           |
| Amazon S3               | アセット・静的ファイル                     |
| Amazon CloudFront       | CDN                                        |
| Terraform               | インフラ as Code                           |
| GitHub Actions          | CI/CD                                      |

開発初期はローカル環境や低コストなクラウド環境で動作させ、利用者数に応じて段階的に拡張します。

ソロ静的公開（案α）は **Cloudflare Pages** を推奨する。Git 連携時の本番ブランチは `develop`（現状の `main` はモノレポ未整備）。手順は [deploy-cloudflare-pages.md](../development/deploy-cloudflare-pages.md)。

## 関連ドキュメント

- [アーキテクチャ概要](./overview.md)
- [通信方針](./communication.md)
- [地理空間アーキテクチャ](./geospatial.md)
- [ゲーム概要](../game-design/overview.md)
