# 地理空間アーキテクチャ

## 現行プレイビュー（Issue #165）

通常プレイは `DioramaGameMap` / Three.js へ移行した。以下の CesiumJS・DEM・PLATEAU の記述は旧地理空間ビューの設計・検証記録であり、通常プレイの描画仕様ではない。

- `dioramaSpace` が経緯度を原点（140.3837, 37.3655）からの局所メートル座標へ変換する。東が +X、北が -Z、高さが +Y。配置確定時は地理座標へ戻す。
- 描画は `loadKoriyamaScene` で同梱したOSM・PLATEAU・国土地理院DEMを読み込み、`geographicWorld` と `geographicTerrain` で作る。範囲は東経140.370–140.398、北緯37.351–37.379。川面・道路・鉄道はOSM形状、建物はPLATEAU 2020年度LOD1を優先し重複するOSM建物を除外する。高さ不明の670棟は仮の6mと記録し、実測と区別する。
- 地面はDEM標高から230mを引いた局所Y座標（1unit=1m、鉛直誇張なし）。建物のLOD1高さを地面に加える。これは地面への整列であり、PLATEAUとDEMの絶対標高の基準年一致を保証しない。水面の微小浮上と橋の3mクリアランスは描画上の仮値で、測深や橋面の実測ではない。
- 指の位置から河岸・水面への交点を計算し、`resolvePlaceablePosition` で河道・河岸への配置可否を判定する。初期の向きは河道方向に合わせる。下部カードからドラッグ、仮配置の移動・回転、確定というスマートフォン専用の導線とする。
- カードと地上施設は同じ3Dモデルを使用する。地理形状はタイル別にバッチ化し、画面外をカリングする。施設の描画とドラッグ位置判定は同じDEM／川面を参照し、欠測を高さ0として扱わない。影は施設変更時に更新する。
- 建物の壁の窓・屋根色・継ぎ目は共有マテリアルによる装飾で、実際の開口位置や階数を示さない。建物形状・高さは変更せず、細部は画面上の密度に合わせて薄くしてちらつきを抑える。
- 水の模様は上流から下流へ並んだ中心線の距離座標で移動させる。阿武隈川の北流は[郡山市の説明](https://www.city.koriyama.lg.jp/soshiki/126/2166.html)に従う。流速は演出値であり実測ではない。旧固定幅リボンの岸の泡と頂点変位を廃止し、OSM水面の境界とピック用形状を変えない。
- `geographicBridges` は両端がDEM範囲内の28橋区間を橋桁・欄干として描く。平面経路は元の全頂点を保持し、橋面は両端標高を距離に沿って補間する推定値（実測でない）。両端が取得範囲外の区間はこの立体化を適用しない。橋脚・アーチの形や桁下高を観測済みとして捏造しない。
- `geographicTrain` は収録されたJR東北本線の連続した一経路を走行する演出。車体位置は元の折れ線上、地面高さは毎フレームDEMで取得し、欠測なら非表示にする。車種・編成・速度・運行時刻は実際の運行情報ではない。地理的な空白を線路で接続しない。
- `geographicLandcover` はOSMの樹木234地点、区域47件を追加する。木のXY位置は元のPointのまま、地面はDEM、高さタグは未検証、未収録の樹高7mと樹冠形状は演出値。樹木は2つのInstancedMeshで描く。区域の外周と穴を保持し、色は分類上の演出。公園範囲を連続した芝生や林とみなしたり、木をランダム配置したりしない。取得時刻・条件・ハッシュはlandcover-metadata.jsonに保存する。
- 洪水の描画は `dioramaInundation` の独立した16m格子で `overflowSites` から伝播させ、濡れたセルだけを描く。共有する角の水深・色を補間し、乾いたセルを外周ポリゴンで塗り潰さない。描画用の経過時間ではなくシミュレーションの経過時間を使い、一時停止中は進めない。丸い危険ピンや一律の影響円は表示しない。この高さ場は教育用の簡略地形であり、実測DEMによる洪水予測ではない。
- 開始視点と小さな対策案内は、治水計算と同じ弱点データを参照する。配置効果の判定は `hydraulicPlacement` / `floodSimulation` に残し、表示だけで防護成功を決めない。

### 実地理への移行要件

実データへの初期接続とドラッグ設置のブラウザ確認を実施した。見た目の磨き込み、橋・鉄道の立体構造、未収録建物の高さ、実機性能評価は未完了。建物・学校・道路・鉄道・橋・水面の平面位置を保持しながら進め、仮値を正確な現況再現と呼ばない。`public/geodata/koriyama` の取得時刻・利用条件・測定年・欠損・推定を参照する。通常ビルドにも実行時データ4種と出典メタデータを同梱する。

施設はドラッグで仮配置し、施設自体のタップで36°ずつ回転する。下部の調整パネルとスライダーは使わず、確定・取消のみ施設横へ表示する。確定済み施設のタップ回転も治水評価へ反映する。表示のみの回転で効果を据え置かない。

## 旧地理空間ビューの設計記録

CesiumJS、国土地理院データ、ゲーム用シミュレーションデータの責務と座標方針を定義する。

## 対象範囲

MVP では福島県郡山市の日本大学工学部周辺を流れる阿武隈川を対象とする。対象範囲の境界、初期カメラ位置、標高範囲は現地確認とデータ検証後に `packages/game-data/maps/koriyama/` へ定義する。

## 責務の分離

| 領域             | 責務                                                           |
| ---------------- | -------------------------------------------------------------- |
| CesiumJS         | 地図・地形・施設の表示、カメラ操作、ドロップ／ピック位置の取得 |
| 国土地理院データ | 背景地図と、採用を決定した場合の標高データの提供               |
| web              | 入力候補の表示、地理座標への変換、サーバー状態の描画           |
| server           | 配置可否、予算、ゲーム進行、水位、浸水、被災度の権威状態       |
| game-data        | 対象範囲、施設配置可能領域、河川領域、計算グリッドとの対応     |

CesiumJS の描画結果をゲーム判定の正としない。表示用地形の解像度や取得状況に左右されず、同じ入力から同じ結果を再現できるよう、サーバーは簡略化したゲーム用データで判定する。

## 座標

- 永続化・REST・WebSocketでは `longitude`、`latitude`、`height` を使用する
- 経度・緯度の単位は度、標高の単位はメートルとする
- 経度は `-180..180`、緯度は `-90..90` の範囲で検証する
- クライアント内部では CesiumJS の `Cartesian3` を使用できるが、通信境界では地理座標へ変換する
- 国土地理院の日本国内タイルは JGD2011、CesiumJS は WGS84 楕円体を基準とするため、MVPで許容する位置精度を実測して記録する
- 浸水計算では対象地域に固定したローカル座標またはグリッドへ変換する。変換処理はサーバー側で一元管理する

既存の `Position { x, y, z }` はプロトタイプ用であり、地理座標型への移行後は通信に使用しない。

## 地図・標高データ

背景は地理院タイルの全国最新写真（シームレス）を既定とし、地名や道路を確認しやすい標準地図へ画面上で切り替えられるようにする。CesiumJS の URL テンプレート対応 Imagery Provider から XYZ タイルとして読み込む。

### リアリティの段階

| 段階 | 表示内容                                         | 状態                                         |
| ---- | ------------------------------------------------ | -------------------------------------------- |
| 1    | 航空写真、斜めカメラ、立体施設、影・大気表現     | 実装済み                                     |
| 2    | 国土地理院DEMを変換した表示用Terrain             | ブラウザ内変換方式で実装済み                 |
| 3    | PLATEAU LOD1建物を3D Tilesとして表示             | 公式全国配信を利用したプロトタイプを実装済み |
| 4    | 専用glTF施設、工事アニメーション、水面・浸水表現 | 複合基本形状まで実装、専用glTFは未実装       |

現在のプロトタイプは、国土地理院の標高PNGをブラウザ内で32×32のHeightmapへ変換し、`CustomHeightmapTerrainProvider`へ渡す。地表タップにはTerrainとの交点を使用する。ズーム14を超える要求では、標高タイルの最大ズーム14を親タイルとして部分サンプリングする。

通常時の建物表示は PLATEAU VIEW の LOD1 3D Tiles をカメラ周辺だけストリーミングする。ゲームのカメラは阿武隈川のプレイ範囲に制限されるため、読込対象は郡山駅からおおむね半径20km以内に収まる。郡山市公式 3D Tiles ZIP 内の **`bldg_texture`**（写真テクスチャ付き）を `apps/web/public/plateau/koriyama-bldg-texture/` に展開する全域取得（約390MB）は、視覚品質検証に限る（`pnpm --filter @civilcraft/web fetch:plateau:full`）。PLATEAU VIEW の `07203-bldg-lod2-texture-latest` は CMS 上のテクスチャ無し LOD2 に解決されるため使わない。テクスチャを消さないよう `Cesium3DTileStyle` の単色上書きは行わない。

初期カメラは日本大学工学部周辺の阿武隈川河道上（おおよそ 140.3837°E, 37.3655°N）。カメラは画面中央の注視点が川中心線から約 950 m 以内に収まるよう操作終了時に補正し（位置クランプはしない）、施設配置は中心線 ± 約 75 m の河道＋河岸コリドー上のみ許可する（市街地・広い氾濫原は不可。判定は `riverPlacement.ts`）。配置可能帯は薄いシアンの Entity コリドーで示し、本川水面は別レイヤの Water マテリアルで描く。写真テクスチャは 2020 年度整備の LOD2 範囲に集中しており、工学部直近は無地のグレー箱が多くなる。建物タイルは `CustomShader`（UNLIT）で両立する。画面空間の色の変化（`dFdx`/`dFdy`）で写真テクスチャ面と無地面を判別し、テクスチャ面はそのまま明るく、無地面は疑似陰影付きのグレー箱として描く。全国 `all-bldg-lod1-2025` には郡山が含まれないため使用しない。地形は **PLATEAU-Terrain**（`https://tile.plateauview.mlit.go.jp/terrain/`、ジオイド補正済み楕円体高）を優先し、失敗時のみ地理院 DEM + 郡山周辺の概算ジオイド高（約 +39.5 m）へフォールバックする。正標高のまま DEM を渡すと建物が浮く。運用版では対象範囲の抽出配信と性能を比較する。表示用3D建物をサーバーの浸水判定へ直接流用しない。

標高の運用方式については次を比較して決定する。

1. 国土地理院の標高タイルを前処理し、対象地域のゲーム用標高グリッドを生成する
2. Cesium対応のTerrainデータを別途用意し、表示用に使用する
3. MVPでは平面または簡略地形を表示し、計算用標高だけを国土地理院データから生成する

国土地理院の標高PNGはCesium Terrain形式ではないため、必ず高さ配列またはTerrain形式へ変換する。現在のランタイム変換は技術検証用であり、運用時はCDN配信、永続キャッシュ、対象範囲外の扱い、欠損値補間を追加検証する。

## 配置フロー

1. クライアントが建設ドックから施設をドラッグし、地表ドロップ位置の地理座標を得る（向きはカメラ heading）
2. 対象範囲内・河道／河岸上かをクライアントで仮判定し、通れば仮配置（プレビュー）を表示する（市街地は拒否。この時点では予算未消費）
3. プレイヤーが向きを調整したうえで確定したとき、施設種別・地理座標・向きをサーバーへ送る（プロトタイプはクライアント確定。キャンセル時は送信しない）
4. サーバーが範囲、地形、重複、担当、予算を検証する
5. サーバーが確定した施設IDと位置・向きを全クライアントへ配信する
6. クライアントが確定状態をCesiumJS上へ描画する（向き・位置の調整は仮配置中のみ。確定後は固定）

## 河道と氾濫原

中心線 `ABUKUMA_RIVER_CENTERLINE` は南→北順（index 0 が下流側）。阿武隈川はこの区間で概ね南へ流れるため、流向ストリークは北→南へアニメーションする。

| レイヤ               | 範囲の目安                                 | 用途                                                         |
| -------------------- | ------------------------------------------ | ------------------------------------------------------------ |
| 本川水面             | 中心線コリドー 約 78〜146 m 幅             | 平常時〜増水の見た目（Water マテリアル＋下流向きストリーク） |
| 配置可能域           | 中心線片岸 **75 m**（全幅約 150 m）        | 河道＋河岸への施設ドロップ。市街地は拒否                     |
| 弱点・越水           | 既知5点＋中心線サンプル。DEM岸高で重み付け | 低い岸・湾曲部が先に溢れる                                   |
| 施設効果             | 河岸距離・向き・標高・弱点近接             | `hydraulicPlacement` / `floodSimulation`                     |
| **高さ場浸水**       | 決壊点から格子上で水位伝播（浅水近似）     | `inundationField` / `inundationVisualization`                |
| **氾濫寸前の氾濫原** | 中心線片岸 **200 m**（全幅約 400 m）       | 水位 3.6 m 超で可視化開始。通常時は非表示                    |
| **越水拡大氾濫原**   | 中心線片岸最大 **320 m**（全幅約 640 m）   | 越水量に応じて外側へ拡大                                     |

氾濫原ポリゴンは `ABUKUMA_RIVER_CENTERLINE` から生成する（`buildCenterlineCorridorRing`）。定数は `floodplainExtent.ts`、描画は `nearOverflowFloodplain.ts`。

浸水表示は決壊地点（`overflowSites`）を起点に、流出方向へプルーム・ストリークを出しつつ、簡易浅水の高さ場（`inundationField`）で低地へ広がる水深バンドを描く。固定座標の市街地浸水ゾーンは使わない。

## 性能方針

- スマートフォン実機を基準に計測する
- 表示範囲、最大ズーム、施設数、同時エフェクト数を制限可能にする
- Reactの再レンダーとCesiumJSの描画ループを分離する
- Cesium Viewerを画面遷移ごとに重複生成しない
- タイル取得失敗時にもゲームUIが操作不能にならないよう状態を分ける

## 利用条件と出典

- 使用する地理院タイルごとに地理院タイル一覧の区分と備考を確認する
- 画面内または常時到達可能な箇所へ国土地理院の出典を表示する
- 加工した標高・地図データには加工した旨と元データを明示する
- 他機関由来のタイルを追加する場合は、各提供元の条件も確認する
- タイルを一括取得・再配布する設計は、利用条件と配信負荷を確認するまで採用しない

参照先:

- [地理院タイル一覧](https://maps.gsi.go.jp/development/ichiran.html)
- [地理院タイルについて](https://maps.gsi.go.jp/development/siyou.html)
- [国土地理院コンテンツ利用規約](https://www.gsi.go.jp/kikakuchousei/kikakuchousei40182.html)
- [CesiumJS UrlTemplateImageryProvider](https://cesium.com/learn/cesiumjs/ref-doc/UrlTemplateImageryProvider.html)
- [CesiumJS CustomHeightmapTerrainProvider](https://cesium.com/learn/cesiumjs/ref-doc/CustomHeightmapTerrainProvider.html)
- [地理院標高タイルの詳細仕様](https://maps.gsi.go.jp/development/demtile.html)
- [PLATEAU 3D都市モデルオープンデータ](https://www.mlit.go.jp/plateau/open-data/)
- [PLATEAU VIEW 3D Tiles配信仕様](https://docs.plateauview.mlit.go.jp/datasets/3d-tiles/)

## 未決事項

- 対象範囲の正確な境界座標と初期カメラ
- 表示用地形をランタイム変換のまま運用するか、事前変換して配信するか
- 計算グリッドのセルサイズと標高データの版
- 河川・道路・住宅地・配置可能領域の作成方法
- オフライン加工データをリポジトリ、S3、CDNのどこで管理するか
- Cesium ionを使用するか、トークン不要の構成にするか
- PLATEAU全国LOD1配信を継続利用するか、郡山市対象範囲を抽出配信するか

## 関連ドキュメント

- [技術スタック](./tech-stack.md)
- [マスターデータ設計](./game-data-master-data.md)
- [game-schema設計](./game-schema-design.md)
- [舞台設定](../civil-engineering/setting.md)
- [CesiumJS導入ロードマップ](../development/cesium-roadmap.md)
